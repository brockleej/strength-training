# RockLog — session handoff

**Parked:** 2026-09-13 (Lee using the phone build). Settings freeze fix + rename to RockLog. **Do not push** until Lee asks after a phone Xcode build.

**Project (only checkout):** `~/Projects/RockLog`  
**App:** RockLog · bundle `com.lee.lift2026` (unchanged — same TestFlight app)  
**Branch:** `cursor/planned-owns-today-9fdc` (PR #8)

> Resume: *“Continue from ~/Projects/RockLog — load docs/SESSION.md. Open RockLog.xcodeproj, scheme RockLog. LAST/PLAN/ACTUAL. Settings should no longer freeze. Do not push until asked.”*  
> HA parked: `~/Documents/Hobbies/Home Automation/docs/HA-SESSION.md`.

---

## One folder

`~/Projects/RockLog`  
(`~/RockLog` and `~/strength-training` both symlink here.)

Open: `~/Projects/RockLog/RockLog.xcodeproj` · scheme **RockLog**

GitHub remote is still `brockleej/strength-training` until that repo is renamed.

---

## Where we left it (2026-09-13)

- Planned queue owns Today while unused sessions wait.
- Focus: LAST / THIS unless a planned workout is loaded, then LAST / PLAN / ACTUAL.
- Send to RockCoach offers Last week.
- Settings: no longer walks the whole planned queue; iCloud line is not a live timer.
- Renamed target/project/folders from strength-training → RockLog. Bundle ID stays `com.lee.lift2026`.

---

**Do not push until Lee asks after phone use.**
